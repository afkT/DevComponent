apply from: rootProject.file(files_component.build_library_gradle)

dependencies {

    // 核心基础依赖库 ( 编译但不参与打包 )
    api project(':core_base_lib')

    // ===================
    // = core - 核心开发库 =
    // ===================

    // 通用工具库
    api project(':core_lib_utils')
}